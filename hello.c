#include <stdio.h>
int main() {

float a , b;
     printf("Enter first number: ");
     scanf("%d" , &a);
     printf("Enter second number: ");
     scanf("%d" , &b);
     
    // sum = hindi + english + science + math + computer;
      // percentage = (sum / 5) * 100;

     //printf("Total: %d", sum);
     //printf("Percentage = %.2f%%\n" , percentage);
if (a == b) {
printf("numbers entered by user are equal");
} 
//if (percentage >= 80) {
//printf("Grade: B");

else {
printf("both numbers are not equal");
}
  
 return 0;
}
